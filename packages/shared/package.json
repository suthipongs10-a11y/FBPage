{
  "name": "@fbpm/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared enums, types and pure domain rules (content state machine, risk levels, permissions)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "5.0.0"
  }
}
