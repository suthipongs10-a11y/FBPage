{
  "name": "@fbpm/facebook-core",
  "version": "0.1.0",
  "private": true,
  "description": "FacebookService — Meta Graph API adapter, metric normalization, retry (AGENTS.md §11, §60, §93)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fbpm/database": "workspace:*",
    "@fbpm/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "5.9.3",
    "vitest": "5.0.0"
  }
}
