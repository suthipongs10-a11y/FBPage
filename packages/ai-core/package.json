{
  "name": "@fbpm/ai-core",
  "version": "0.1.0",
  "private": true,
  "description": "AI Gateway — provider adapters (anthropic/openai/gemini/openrouter/compatible), tool loop, structured output, cost estimate (AGENTS.md §4, §5, §50)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fbpm/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "5.9.3",
    "vitest": "5.0.0"
  }
}
