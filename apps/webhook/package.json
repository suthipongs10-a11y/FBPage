{
  "name": "@page-os/webhook",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/server.js",
    "dev": "node --watch dist/server.js"
  },
  "dependencies": {
    "@page-os/core": "workspace:*",
    "@page-os/inbox": "workspace:*",
    "@page-os/queue": "workspace:*",
    "fastify": "^5.11.2"
  }
}
